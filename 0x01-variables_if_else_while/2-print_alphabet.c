#include <stdio.h>

/**
 * main - Prints the alphabet.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alp[26] = "abcdefghijklmnopqrstuvwz";
	int i;

	for (i = 0, ii < 26; i++)
	{
		putchar(alp[i]);
	}
	putchar('\n');
	return (0);
}
